// js/firebase/roomManager.js (Testing the import connection)

// เราจะสร้างฟังก์ชันเปล่าๆ ขึ้นมาเพื่อทดสอบ
export function createRoom() {
    alert("createRoom function was called!");
}

export function loadAndDisplayRooms() {
    alert("loadAndDisplayRooms function was called!");
}

export function handlePasswordSubmit() {
    // ฟังก์ชันนี้ยังไม่ต้องทำอะไร
}

export function joinRoom() {
    // ฟังก์ชันนี้ยังไม่ต้องทำอะไร
}
